# R1-FND-002: Implement reproducible local infrastructure and configuration

Non-runnable tracking group. Ralph executes its ordered leaves, never this issue.

Implement Docker Compose PostgreSQL, MinIO, and Mailpit; runtime environment schemas; process lifecycle; deterministic clock and identifier adapters; and the production guard against fixed identity.

Release requirements: R1-LIFE-001, R1-OPS-001.

## Required children

- R1-FND-002-A
- R1-FND-002-B

## Rollup

Done only when every child is independently verified and integrated, combined parent behavior and requirement coverage are confirmed, and no accepted scope remains unowned. Labels and checkboxes alone cannot establish completion.
