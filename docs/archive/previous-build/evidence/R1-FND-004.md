# R1-FND-004 Verification Evidence

> Historical report only. The referenced implementation has been deleted; these results do not prove current completion. See [archive context](../README.md).

Status: superseded after implementation removal on September 9, 2026. Previous status: complete

## Requirement traceability

| Requirement | Implemented artifacts | Verification |
| --- | --- | --- |
| `R1-AUTH-001` | Generated authorization matrix, OpenAPI error components, stable error definitions, artifact registry, and release traceability manifest | complete generated requirement trace, missing-trace negative test, and `pnpm contracts:check` |
| `R1-COMP-001` | Versioned `GoldenVectorEnvelope` runtime and JSON schemas plus deterministic vector index | two-run byte equality, vector descriptor validation, and incompatible schema-change negative test |
| `R1-PRIV-001` | Closed stable error envelope, safe generated error catalog, authorization projections, and source/evidence-linked traceability | generated-directory ownership test, invalid reference validation, and `pnpm contracts:check` |

The generated machine-readable manifest (historical path: `contracts/traceability/manifest.json`) and reviewer report (historical path: `contracts/traceability/report.md`) extend this item-level evidence across every accepted Release 1 requirement and work-item status.

## Declared outputs and interfaces

- `@docket/contracts` exports the runtime schemas and types for `ContractSource`, `GeneratorResult`, `TraceabilityManifest`, `GoldenVectorEnvelope`, and `StableErrorEnvelope`, plus the single `runContractGenerator` interface.
- `pnpm contracts:generate` writes deterministic projections from module-owned sources, the Release 1 contract, the authoritative work graph, and `contracts/traceability/manifest.source.json`.
- Fifteen owned generated artifacts cover OpenAPI, JSON Schema, the TypeScript client, state and authorization matrices, module and state diagrams, the stable error catalog, field references, the golden-vector index, the artifact registry, and machine- and human-readable traceability.
- `pnpm contracts:check` rejects byte drift, unowned files in generated directories, duplicate source ownership, incomplete Release 1 traces, invalid source/evidence/migration/vector references, and incompatible JSON Schema or OpenAPI changes.
- Stable failures are `CONTRACT_DRIFT`, `REQUIREMENT_UNTRACED`, `ARTIFACT_UNOWNED`, and `CONTRACT_BREAKING_CHANGE`; diagnostics do not disclose source contents or secrets.

## Passing evidence

- `pnpm install --frozen-lockfile --config.confirm-modules-purge=false` — passed for all 19 workspace projects without lockfile drift.
- `pnpm build` — passed generated-contract drift validation, strict TypeScript compilation, and the server-rendered web production build.
- `pnpm check` — passed formatting, ESLint, strict types, dependency boundaries and their negative fixture, workspace and infrastructure assertions, runtime configuration, migration validation, contract validation, and 23 unit/accessibility tests across 8 files.
- `pnpm test` — passed 23 unit/accessibility tests, 2 Docker-backed isolated PostgreSQL integration tests, and the reserved end-to-end tier.
- Two-run byte equality — two consecutive writes produced identical bytes for all 15 generated artifacts.
- Dirty-tree drift — mutating a generated artifact was rejected with `CONTRACT_DRIFT`.
- Missing trace — removing an accepted Release 1 requirement trace was rejected with `REQUIREMENT_UNTRACED`.
- Artifact ownership — adding an undeclared file to a generator-owned directory was rejected with `ARTIFACT_UNOWNED`.
- Compatibility — removing a generated schema property without a versioned replacement was rejected with `CONTRACT_BREAKING_CHANGE`.

## Completion

Every declared output exists and every item-level acceptance check passes. `R1-FND-004` is `done`; only its immediate dependent, `R1-FND-005`, is promoted to `ready`.
