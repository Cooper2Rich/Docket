# R1-FND-005 Verification Evidence

> Historical report only. The referenced implementation has been deleted; these results do not prove current completion. See [archive context](../README.md).

Status: superseded after implementation removal on September 9, 2026. Previous status: complete

## Requirement traceability

| Requirement | Implemented artifacts | Verification |
| --- | --- | --- |
| `R1-OPS-001` | Required GitHub Actions workflow, pinned setup action, CI policy validator, security and accessibility gates, build-artifact manifest, cache and retention policy, and branch-protection contract | clean `pnpm ci:full`, per-check negative fixtures, zero-vulnerability audit, and deterministic artifact manifest |

The generated machine-readable manifest (historical path: `contracts/traceability/manifest.json`) and reviewer report (historical path: `contracts/traceability/report.md`) link this item and its evidence to the accepted operations requirement.

## Declared outputs and interfaces

- `.github/workflows/required-checks.yml` exposes eleven stable required contexts for frozen installation, build, check, unit, integration, end-to-end, contract, migration, security, accessibility, and artifact evidence.
- `.github/actions/setup-docket/action.yml` installs exact pnpm 11.19.0 and Node 24.19.0, restores only the lockfile-keyed pnpm content-addressed store, and uses immutable external action revisions.
- `pnpm ci:check` rejects missing or renamed checks, local-command drift, non-frozen installation, mutable actions, excessive workflow permissions, missing timeouts, and weakened artifact handling with `REQUIRED_CHECK_MISSING` or `CI_LOCAL_PARITY_FAILED`.
- `pnpm security:check`, `pnpm accessibility:check`, and `pnpm artifacts:check` are documented local seams for their corresponding CI checks. Artifact verification emits a deterministic SHA-256 manifest for the deployable outputs.
- [The branch-protection contract](../../../operations/github-branch-protection.md) defines the exact contexts, ruleset posture, local parity, cache boundaries, seven-day verification-artifact retention, and safe change sequence.

## Negative evidence

`tests/fixtures/ci/required-checks.json` names every required check. The CI policy tests remove each job and alter each documented command independently, proving all eleven `REQUIRED_CHECK_MISSING` paths and all eleven `CI_LOCAL_PARITY_FAILED` paths. A separate negative test rejects a mutable external action tag, and build-artifact tests reject incomplete output.

## Passing evidence

- `pnpm install --frozen-lockfile` reconstructed all 19 workspace projects from the exact lockfile.
- `pnpm build` passed deterministic contract validation, strict TypeScript compilation, and the server-rendered web production build.
- `pnpm check` passed formatting, ESLint, strict types, dependency boundaries and their negative fixture, workspace and infrastructure assertions, runtime configuration, migration and contract validation, CI policy, and 29 unit tests across 10 files.
- `pnpm test:unit` passed all 29 tests; `pnpm test:integration` passed both isolated Docker-backed PostgreSQL migration tests; and `pnpm test:e2e` passed the current reserved end-to-end tier.
- `pnpm security:check` passed source credential and immutable-action scans and reported no known dependency vulnerabilities. Nx 23.2.0 replaced vulnerable Nx transitive versions, and the workspace pins patched `qs` 6.16.0 for React Router's Express adapter.
- `pnpm accessibility:check` passed the dedicated automated WCAG 2.2 AA primitive smoke suite.
- `pnpm artifacts:check` verified and hashed 135 deployable files. Two consecutive `build-artifacts/manifest.json` writes had SHA-256 `4761FCFB81968F4DCBFF88C9DFE4DEAADEA9DDEA6EEB3AFCC01ED96DA1452B9E`.
- `pnpm ci:full` passed the entire sequence in one clean run, including frozen install, build, all checks and test tiers, contract and migration integrity, zero-vulnerability security, accessibility, and artifact validation.
- The checked-in fixture produced 11 `REQUIRED_CHECK_MISSING` failures and 11 `CI_LOCAL_PARITY_FAILED` failures; the mutable-action negative test also failed as required.

## Completion

Every declared output exists and every item-level acceptance check passes. `R1-FND-005` is `done`; only its immediate dependent, `R1-IDA-001`, is promoted to `ready`.
